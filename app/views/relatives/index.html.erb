<p id="notice"><%= notice %></p>

<h1>Relatives</h1>

<table class='ui celled table'>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @relatives.each do |relative| %>
      <tr>
        <td><%= relative.name %></td>
        <td><%= link_to 'Visualizar', relative %></td>
        <td><%= link_to 'Editar', edit_relative_path(relative) %></td>
        <td><%= link_to 'Excluir', relative, method: :delete, data: { confirm: 'Você tem certeza que deseja excluir?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Relative', new_relative_path %>
