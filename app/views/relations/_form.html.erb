<%= form_with(model: relation, local: true, class: 'ui form') do |form| %>
  <% if relation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(relation.errors.count, "error") %> prohibited this relation from being saved:</h2>

      <ul>
      <% relation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="ui divider"></div>
  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :desapeared_id %>
    <%= form.text_field :desapeared_id %>
  </div>

  <div class="field">
    <%= form.label :relative_id %>
    <%= form.text_field :relative_id %>
  </div>

 <div class="row">
    <%= link_to relations_path, class: 'button' do%>
        <i>VOLTAR</i> 
        </div>
     <% end %>
<% end %>



