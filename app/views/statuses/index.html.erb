<p id="notice"><%= notice %></p>

<h1>Statuses</h1>

<table class='ui celled table'>
  <thead>
    <tr>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @statuses.each do |status| %>
      <tr>
        <td><%= status.description %></td>
        <td><%= link_to 'Visualizar', status %></td>
        <td><%= link_to 'Editar', edit_status_path(status) %></td>
        <td><%= link_to 'Excluir', status, method: :delete, data: { confirm: 'Você tem certeza que deseja excluir?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Status', new_status_path %>
