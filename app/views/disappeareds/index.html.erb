<p id="notice"><%= notice %></p>

<h1>Disappeareds</h1>

<table class='ui celled table'>
  <thead>
    <tr>
      <th>User</th>
      <th>Name</th>
      <th>Age</th>
      <th>Sex</th>
      <th>Birthdate</th>
      <th>Date desapearence</th>
      <th>Street</th>
      <th>Mother</th>
      <th>Dad</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @disappeareds.each do |disappeared| %>
      <tr>
        <td><%= disappeared.user %></td>
        <td><%= disappeared.name %></td>
        <td><%= disappeared.age %></td>
        <td><%= disappeared.sex %></td>
        <td><%= disappeared.birthdate %></td>
        <td><%= disappeared.date_desapearence %></td>
        <td><%= disappeared.street %></td>
        <td><%= disappeared.mother %></td>
        <td><%= disappeared.dad %></td>
        <td><%= link_to 'Visualizar', disappeared %></td>
        <td><%= link_to 'Editar', edit_disappeared_path(disappeared) %></td>
        <td><%= link_to 'Excluir', disappeared, method: :delete, data: { confirm: 'Você tem certeza que deseja excluir?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Disappeared', new_disappeared_path %>
